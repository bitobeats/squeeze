import { createTheme } from "@mui/material";

export const greenButtonTheme = createTheme({
    palette: {
        primary: {
            main: "rgba(85, 211, 154, 1)",
        },
    },
});

export const blackTheme = createTheme({
    palette: {
        primary: {
            main: "#000000",
        },
    },
});

export const redTheme = createTheme({
    palette: {
        primary: {
            main: "#f44336",
        },
    },
});
