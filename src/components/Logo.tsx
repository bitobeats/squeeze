import { Typography } from "@mui/material";

function Logo() {
    return (
        <Typography fontSize={30} fontFamily={'"Baloo 2"'} sx={{ flexGrow: 1 }}>
            squeeze
        </Typography>
    );
}

export default Logo;